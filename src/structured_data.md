# Structured Data

In Rust, the grouping of data types into a higher level abstraction is called a `struct`.

* 🦀 Rust: `struct`
* 💎 Ruby: `class`
* ☕ Java: `class`
* ©️ C/C++: `struct`, `class`

```rust
pub struct Name {
    field: Type
}
```
